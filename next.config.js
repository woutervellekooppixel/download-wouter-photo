/** @type {import('next').NextConfig} */
const nextConfig = {
  images: {
    domains: ['pub-0259df1e2f8a4519882e857eebaab6fa.r2.dev'],
  },
  // Als je `matcher` bedoelt voor middleware, moet dat in `middleware.ts`, niet hier
};

module.exports = nextConfig;